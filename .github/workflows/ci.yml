name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: jarvis_test
          POSTGRES_USER: jarvis
          POSTGRES_PASSWORD: jarvis
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U jarvis -d jarvis_test"
          --health-interval 3s
          --health-timeout 3s
          --health-retries 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: apps/web/package-lock.json

      - name: Install uv
        run: python -m pip install uv==0.11.29

      - name: Bootstrap twice
        env:
          POSTGRES_PORT: "55432"
          TEST_DATABASE_URL: postgresql://jarvis:jarvis@localhost:55432/jarvis
        run: |
          make bootstrap
          make bootstrap
          make stop

      - name: Verify installed pre-commit hook
        env:
          UV_OFFLINE: "true"
          npm_config_offline: "true"
        run: "$(git rev-parse --git-dir)/hooks/pre-commit"

      - name: Verify
        env:
          TEST_DATABASE_URL: postgresql://jarvis:jarvis@localhost:5432/jarvis_test
          UV_OFFLINE: "true"
          npm_config_offline: "true"
        run: make verify

      - name: Validate Compose
        run: docker compose config --quiet

      - name: Build containers
        run: docker compose build

      - name: Run disposable demo twice
        env:
          POSTGRES_PORT: "55432"
        run: |
          make demo
          make demo
